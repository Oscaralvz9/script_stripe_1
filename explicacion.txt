En este script en primer lugar se instalan las bibliotecas necesarias para ejecutar el resto de código. Time para poder usar el sleep y poder ejecutar el script en un intervalo de tiempo, stripe para comunicarse con la API de stripe... Luego nos autenticamos ante la api de stripe con el token y seguidamente definimos la funcion que se comunicara con la API de proxmox para crea el contenedor. Esta parte ya no se explica, porque ya está hecho en otro documento. La ultima parte de este script lo que hace es ejecutar la función de crear el contenedor unicamente si al pedir a través de la API de stripe el id de pago del último pago, este no coincide con el almacenado en el archivo de texto. Una vez hecho esto se actualizará el archivo de texto.
